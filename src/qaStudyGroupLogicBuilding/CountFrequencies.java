package qaStudyGroupLogicBuilding;

public class CountFrequencies {
	/*1step count frquencies 
	 * 2 duplicate character 
	 * 3- repeated charachters 
	 * 4-print 2
	 * 
	 * non repeated characters 
	 * pseudo code 
	 * String str ="swiss";
	 *      char ch =replace.TocharArray; ch=[i][l][o][v][e][j][a][v][a] ch-->[0]--> i....
	 *      for(char c:  ch ) { c-->[i][l][o][v][e][j][a][v][a] traverse 
	 *      for(char c : ch) { c-->i
	 *      
			if(map.containsKey(c)) { //no ist Iteration its not there 
				
				 * nonRepeatedCh++
				 *   map.get(c) -->l
				 *   if(
			} else if{
				map.put(c, 1); // c is my key --> e  value 1 a, 1 // now my map exist 2n
			} else {
			
	 * 
	 * 
	 */

}
